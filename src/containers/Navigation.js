import React, { Fragment } from 'react';
import { TopNav, SideNav } from './Nav';

const Navigation = () => {
    return (
        <Fragment>
            <TopNav />
            <SideNav />
        </Fragment>
    );
};

export default Navigation;
